query GetPictures {
    GetPictures {
        path
        rating
    }
}

query GetPaths {
    GetPaths {
        id
        path
        updatedAt
    }
}

mutation AddPath {
    AddPath(input: {path: "C:\\src\\github\\laosyne\\pictures"}) {
        id
        path
        createdAt
    }
}

mutation ScanPaths {
    ScanPaths
}

mutation AddToRating {
    AddToRating(pictureId: 11) {
        id
        rating
    }
}

mutation CreateMatch {
    CreateMatch {
        playerOne {
            id
            rating
            deviation
            updatedAt
        }
        playerTwo {
            id
            rating
            deviation
            updatedAt
        }
    }
}

mutation ReportMatchResult {
    ReportMatchResult(input: {winnerId: 1, loserId: 2})
}